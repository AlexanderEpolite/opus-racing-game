{
  "name": "opus-racing-game",
  "version": "1.0.0",
  "description": "Sunset Ridge GP — a 3D arcade kart racer in the browser",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "serve": "npx --yes serve -l 8080 .",
    "server": "bun run server/server.ts",
    "server:node": "node --experimental-strip-types server/server.ts"
  }
}
